package tp.pr2.ByteCode;

import tp.pr2.CPU.CPU;

public abstract class Arithmetics extends ByteCode{
	private final static Arithmetics[]
	public boolean execute(CPU cpu){
		
	}
	public ByteCode parse(String[] s){
		if (s.length == 1){
			
		}
		else return null;
	}
}
